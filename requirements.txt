# Pinned to versions with prebuilt wheels for Python 3.14 (verified working).
# The older pinned versions of pydantic-core have no 3.14 wheel and fail to
# compile from source, so use these floors or newer.
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
python-multipart>=0.0.32
pydantic>=2.13.4
python-dotenv>=1.1.1
sentence-transformers>=5.6.0
langchain-text-splitters>=1.1.2
pypdf>=6.14.2
python-docx>=1.2.0
numpy>=2.5.1
boto3>=1.39.0

# Optional: only needed to run the Langflow custom component (langflow_components/).
langflow>=1.1.0
